# game-prototype

## 기능 관리 규칙

모든 기능 요청은 `game-card/docs/기능.md`로 관리한다.

작업 순서:

1. 작업 전 `game-card/docs/기능.md`를 읽는다.
2. 코드를 수정한다.
3. 수정한 내용을 `game-card/docs/기능.md`에 기록한다. 구현이 끝난 항목은 체크박스를 `- [x]`로 바꾼다.

사용자가 `기능.md`를 거치지 않고 그냥 요청하는 경우:

- 먼저 `기능.md`에 항목으로 기록한 뒤 작업한다.
- 또는 사용자에게 `기능.md`에 먼저 기록할 것을 권장한다.
